name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: gradle/actions/setup-gradle@v4

      - uses: android-actions/setup-android@v3
        with:
          packages: "platform-tools platforms;android-37"

      - name: Version from tag
        run: |
          name="${GITHUB_REF_NAME#v}"
          core="${name%%-*}"
          IFS='.' read -r major minor patch _ <<EOF
          $core
          EOF
          major=${major:-0}
          minor=${minor:-0}
          patch=${patch:-0}
          major=${major//[^0-9]/}
          minor=${minor//[^0-9]/}
          patch=${patch//[^0-9]/}
          code=$((10#${major:-0} * 10000 + 10#${minor:-0} * 100 + 10#${patch:-0}))
          if [ "$code" -le 0 ]; then code=1; fi
          echo "VERSION_NAME=$name" >> "$GITHUB_ENV"
          echo "VERSION_CODE=$code" >> "$GITHUB_ENV"

      - name: Decode keystore
        if: ${{ secrets.SIGNING_STORE_BASE64 != '' }}
        env:
          SIGNING_STORE_BASE64: ${{ secrets.SIGNING_STORE_BASE64 }}
        run: |
          echo "$SIGNING_STORE_BASE64" | base64 -d > "$RUNNER_TEMP/release.jks"
          echo "SIGNING_STORE_FILE=$RUNNER_TEMP/release.jks" >> "$GITHUB_ENV"

      - name: Test and assemble
        env:
          SIGNING_STORE_PASSWORD: ${{ secrets.SIGNING_STORE_PASSWORD }}
          SIGNING_KEY_ALIAS: ${{ secrets.SIGNING_KEY_ALIAS }}
          SIGNING_KEY_PASSWORD: ${{ secrets.SIGNING_KEY_PASSWORD }}
        run: >
          ./gradlew --no-daemon
          :app:testDebugUnitTest
          :app:assembleRelease
          -PversionName="${VERSION_NAME}"
          -PversionCode="${VERSION_CODE}"

      - name: Name the APK
        run: |
          apk=$(find app/build/outputs/apk/release -name '*.apk' | head -n 1)
          test -n "$apk"
          cp "$apk" "darkroom-${VERSION_NAME}.apk"

      - uses: softprops/action-gh-release@v2
        with:
          files: darkroom-*.apk
          generate_release_notes: true
